---
title: "三数之和与 N 数之和：排序+对撞指针的组合拳"
date: 2026-04-28
tags: [算法, 双指针, 对撞指针, 三数之和, 四数之和, 去重, LeetCode, 面试]
aliases: [三数之和, 3Sum, 4Sum, N数之和]
---

# 三数之和与 N 数之和：排序+对撞指针的组合拳

## 摘要

3Sum 是面试中出现频率最高的双指针题之一，也是去重逻辑最容易写错的题之一。本文从两数之和出发，推导三数之和的"降维"思路，精确分析三处去重逻辑的必要性，然后推广到 3Sum Closest 和 4Sum，最后抽象出能处理任意 N 数之和的递归框架。

---

## 第 1 章 从两数之和到三数之和的降维思路

### 1.1 暴力枚举的代价

三数之和的暴力解法是三重循环枚举所有三元组 $(i, j, k)$，时间复杂度 $O(n^3)$。对于 $n = 3000$（LeetCode 3Sum 的约束），$O(n^3) \approx 2.7 \times 10^{10}$，远超时间限制。

如何优化？回想 Two Sum II 的思路：有序数组 + 对撞指针，时间从 $O(n^2)$ 降到 $O(n)$。

三数之和可以用类似的策略：**固定最左边的数 `nums[i]`，对剩余数组 `nums[i+1..n-1]` 用对撞指针找两数之和 `target - nums[i]`**。

- 外层循环：枚举 `i`，从 0 到 `n-3`，$O(n)$
- 内层对撞指针：对有序数组找两数之和，$O(n)$

总时间复杂度：$O(n) \times O(n) = O(n^2)$。比暴力 $O(n^3)$ 快一个数量级。

**前提条件**：内层必须对有序数组做对撞指针，因此需要先对整个数组排序（$O(n \log n)$）。总复杂度取 $\max(O(n \log n), O(n^2)) = O(n^2)$，排序不是瓶颈。

### 1.2 降维的通用逻辑

这个"固定最外层，内层用更快的算法"的思路，是 N 数之和的通用解题框架：

- **2Sum（有序）**：对撞指针，$O(n)$
- **3Sum**：固定一层，内部退化为 2Sum，$O(n^2)$
- **4Sum**：固定两层，内部退化为 2Sum，$O(n^3)$
- **kSum**：固定 $k-2$ 层，内部退化为 2Sum，$O(n^{k-1})$

---

## 第 2 章 LeetCode 15：三数之和

### 2.1 题目

**LeetCode 15 - 3Sum**（中等）

给你一个整数数组 `nums`，判断是否存在三元组 `[nums[i], nums[j], nums[k]]` 满足 `i != j`、`i != k` 且 `j != k`，同时还满足 `nums[i] + nums[j] + nums[k] == 0`。

请你返回所有和为 `0` 且不重复的三元组。**注意：答案中不可以包含重复的三元组。**

```
输入：nums = [-1,0,1,2,-1,-4]
输出：[[-1,-1,2],[-1,0,1]]

输入：nums = [0,1,1]
输出：[]

输入：nums = [0,0,0]
输出：[[0,0,0]]
```

### 2.2 完整解法

```java
public List<List<Integer>> threeSum(int[] nums) {
    List<List<Integer>> result = new ArrayList<>();
    int n = nums.length;

    // 排序是对撞指针的前提
    Arrays.sort(nums);

    for (int i = 0; i < n - 2; i++) {
        // 剪枝：最小的三个数之和已经大于 0，不可能有解
        if (nums[i] > 0) break;

        // 去重（外层）：跳过与上一个 i 相同的值，避免重复三元组
        if (i > 0 && nums[i] == nums[i - 1]) continue;

        int left = i + 1, right = n - 1;
        int target = -nums[i];  // 在 nums[i+1..n-1] 中找两数之和为 target

        while (left < right) {
            int sum = nums[left] + nums[right];
            if (sum == target) {
                result.add(Arrays.asList(nums[i], nums[left], nums[right]));

                // 去重（内层左）：跳过与当前 left 相同的值
                while (left < right && nums[left] == nums[left + 1]) left++;
                // 去重（内层右）：跳过与当前 right 相同的值
                while (left < right && nums[right] == nums[right - 1]) right--;

                left++;
                right--;
            } else if (sum < target) {
                left++;
            } else {
                right--;
            }
        }
    }
    return result;
}
```

### 2.3 三处去重逻辑的精确分析

这是整道题最容易写错的部分，需要仔细推导每处去重的必要性。

#### 去重一：外层循环的 `if (i > 0 && nums[i] == nums[i - 1]) continue`

**问题**：为什么需要这行？

假设数组排序后是 `[-1, -1, 0, 1, 2]`，target = 0。

当 `i = 0` 时，`nums[i] = -1`，内层找到 `[-1, 0, 1]`。
当 `i = 1` 时，`nums[i] = -1`，内层还会找到 `[-1, 0, 1]`——重复了！

**原因**：两次迭代的 `nums[i]` 值相同，内层的搜索范围不同（后者比前者少了第一个 -1），但对于 target 不依赖 `i` 位置的问题，内层会找到相同的答案。

**解决方案**：当 `nums[i] == nums[i-1]` 时跳过，保证固定的第一个数值每种只处理一次。

**为什么是 `nums[i-1]` 而不是 `nums[i+1]`？**

如果写成 `nums[i] == nums[i+1]`，当 `i = 0` 时，`nums[0] = -1`，`nums[1] = -1`，会跳过 `i = 0`，导致这种 `-1` 完全不处理，漏掉所有包含 `-1` 的答案。

正确逻辑：跳过的是"已经处理过了"（看前面的 `i-1`），而不是"下面还有相同的"（看后面的 `i+1`）。

> [!warning] 生产避坑：去重比较的方向
> 外层去重必须与**上一个元素**比较（`nums[i-1]`），不能与**下一个元素**比较（`nums[i+1]`）。前者表示"这个值之前处理过了，跳过"；后者表示"这个值之后还有重复，跳过"，但这样会把第一次出现的值也跳过，导致漏掉答案。

#### 去重二和三：内层的 `while (left < right && nums[left] == nums[left + 1]) left++`

**问题**：找到一个有效三元组后，为什么需要跳过 `left` 和 `right` 的重复值？

假设数组是 `[-2, 0, 0, 2, 2]`，target = 2（外层 `nums[i] = -2`）。

找到 `[0, 2]` 后（`left=1, right=4`，`nums[1]+nums[4]=2`），如果直接 `left++, right--`，下一步是 `left=2, right=3`，`nums[2]+nums[3]=0+2=2`，还是找到 `[0, 2]`！产生重复三元组 `[-2, 0, 2]`。

所以找到答案后，需要：
1. 将 `left` 向右推，跳过所有与 `nums[left]` 相同的值（`while nums[left] == nums[left+1]`，然后 `left++`）
2. 将 `right` 向左推，跳过所有与 `nums[right]` 相同的值（`while nums[right] == nums[right-1]`，然后 `right--`）

注意：跳过重复值后，还需要额外执行一次 `left++` 和 `right--`（代码最后两行），因为当前找到的答案中的元素本身也需要跳过。

**内层 while 的边界**：`while (left < right && ...)` 中的 `left < right` 同样不能省略，防止指针越界。

### 2.4 剪枝优化

```java
// 剪枝一：nums[i] > 0 时，三个非负数之和不可能为 0（前提是已排序）
if (nums[i] > 0) break;

// 剪枝二（可选）：最小的三个数之和大于 0，后续不可能有解
if (nums[i] + nums[i+1] + nums[i+2] > 0) break;

// 剪枝三（可选）：当前 nums[i] 加上最大的两个数仍然小于 0，跳过当前 i
if (nums[i] + nums[n-2] + nums[n-1] < 0) continue;
```

剪枝一是最重要的：排序后如果 `nums[i] > 0`，`nums[i+1]` 和 `nums[i+2]` 也 ≥ `nums[i] > 0`，三数之和必然大于 0，直接 `break` 终止外层循环。

---

## 第 3 章 LeetCode 16：最接近的三数之和

### 3.1 题目

**LeetCode 16 - 3Sum Closest**（中等）

给你一个长度为 `n` 的整数数组 `nums` 和一个目标值 `target`。请你从 `nums` 中选出三个整数，使它们的和与 `target` 最为接近。返回这三个数的和。

假定每组输入只存在恰好一个解。

```
输入：nums = [-1,2,1,-4], target = 1
输出：2
解释：与 target 最接近的和是 2 (-1 + 2 + 1 = 2)。

输入：nums = [0,0,0], target = 1
输出：0
```

### 3.2 解法：对撞指针 + 全局最近值维护

思路与 3Sum 完全相同，区别在于：不需要找和恰好等于 target，而是找和最接近 target。把"和是否等于 target"改成"比较当前和与 target 的差值绝对值"。

```java
public int threeSumClosest(int[] nums, int target) {
    Arrays.sort(nums);
    int n = nums.length;
    int closest = nums[0] + nums[1] + nums[2];  // 初始值设为前三个数之和

    for (int i = 0; i < n - 2; i++) {
        // 外层去重（可选，不影响正确性，但能提速）
        if (i > 0 && nums[i] == nums[i - 1]) continue;

        int left = i + 1, right = n - 1;
        while (left < right) {
            int sum = nums[i] + nums[left] + nums[right];

            // 更新全局最近值
            if (Math.abs(sum - target) < Math.abs(closest - target)) {
                closest = sum;
            }

            if (sum == target) {
                return sum;  // 完美匹配，直接返回
            } else if (sum < target) {
                left++;   // 和偏小，左指针右移
            } else {
                right--;  // 和偏大，右指针左移
            }
        }
    }
    return closest;
}
```

**时间复杂度**：$O(n^2)$，与 3Sum 相同。

与 3Sum 相比，这道题不需要处理重复三元组（因为只需要返回一个和值），代码更简洁。

### 3.3 与 3Sum 的对比

| | 3Sum | 3Sum Closest |
|--|------|-------------|
| 目标 | 找所有和为 0 的三元组 | 找和最接近 target 的三数之和 |
| 去重 | 必须，三处去重逻辑 | 不需要 |
| 提前返回 | 找到就添加，继续找 | 和等于 target 时立即返回 |
| 额外状态 | 无 | 维护 `closest` 全局变量 |

---

## 第 4 章 LeetCode 18：四数之和

### 4.1 题目

**LeetCode 18 - 4Sum**（中等）

给你一个由 `n` 个整数组成的数组 `nums`，和一个目标值 `target`。请你找出并返回满足下述全部条件且**不重复**的四元组 `[nums[a], nums[b], nums[c], nums[d]]`（若两个四元组元素一一相等，则认为两个四元组重复）。

```
输入：nums = [1,0,-1,0,-2,2], target = 0
输出：[[-2,-1,1,2],[-2,0,0,2],[-1,0,0,1]]

输入：nums = [2,2,2,2,2], target = 8
输出：[[2,2,2,2]]
```

### 4.2 解法：两层循环 + 对撞指针

固定 `nums[i]` 和 `nums[j]`，对剩余数组用对撞指针找两数之和 `target - nums[i] - nums[j]`。

```java
public List<List<Integer>> fourSum(int[] nums, int target) {
    List<List<Integer>> result = new ArrayList<>();
    int n = nums.length;
    Arrays.sort(nums);

    for (int i = 0; i < n - 3; i++) {
        // 外层去重
        if (i > 0 && nums[i] == nums[i - 1]) continue;
        // 剪枝：最小四数之和已经大于 target
        if ((long) nums[i] + nums[i+1] + nums[i+2] + nums[i+3] > target) break;
        // 剪枝：当前 i 加最大三个数仍然小于 target，跳过
        if ((long) nums[i] + nums[n-3] + nums[n-2] + nums[n-1] < target) continue;

        for (int j = i + 1; j < n - 2; j++) {
            // 第二层去重
            if (j > i + 1 && nums[j] == nums[j - 1]) continue;
            // 剪枝（第二层）
            if ((long) nums[i] + nums[j] + nums[j+1] + nums[j+2] > target) break;
            if ((long) nums[i] + nums[j] + nums[n-2] + nums[n-1] < target) continue;

            int left = j + 1, right = n - 1;
            long twoTarget = (long) target - nums[i] - nums[j];

            while (left < right) {
                long sum = nums[left] + nums[right];
                if (sum == twoTarget) {
                    result.add(Arrays.asList(nums[i], nums[j], nums[left], nums[right]));
                    while (left < right && nums[left] == nums[left + 1]) left++;
                    while (left < right && nums[right] == nums[right - 1]) right--;
                    left++;
                    right--;
                } else if (sum < twoTarget) {
                    left++;
                } else {
                    right--;
                }
            }
        }
    }
    return result;
}
```

**注意整数溢出**：四个 `int` 相加可能溢出，需要用 `long` 进行计算（如 `(long) nums[i] + nums[j] + ...`）。

**时间复杂度**：$O(n^3)$（两层循环 $O(n^2)$ + 内层对撞指针 $O(n)$）。

### 4.3 4Sum 的去重逻辑

4Sum 有两层外层循环，每层都需要去重，逻辑与 3Sum 的外层去重完全相同：

- `i > 0 && nums[i] == nums[i-1]`：第一层，防止相同的 `nums[i]` 处理两次
- `j > i+1 && nums[j] == nums[j-1]`：第二层，注意条件是 `j > i+1`（不是 `j > 0`），因为 `j` 从 `i+1` 开始，`j = i+1` 时是第一次进入第二层循环，不能去重

---

## 第 5 章 通用 kSum 递归框架

### 5.1 框架设计

当题目变成 5Sum、6Sum 时，继续手动增加循环层数是不现实的。可以设计一个递归框架：

```java
// kSum 递归框架
List<List<Integer>> kSum(int[] nums, int target, int k, int start) {
    List<List<Integer>> result = new ArrayList<>();
    int n = nums.length;

    // 基础情况：2Sum，用对撞指针
    if (k == 2) {
        int left = start, right = n - 1;
        while (left < right) {
            int sum = nums[left] + nums[right];
            if (sum == target) {
                result.add(new ArrayList<>(Arrays.asList(nums[left], nums[right])));
                while (left < right && nums[left] == nums[left + 1]) left++;
                while (left < right && nums[right] == nums[right - 1]) right--;
                left++;
                right--;
            } else if (sum < target) {
                left++;
            } else {
                right--;
            }
        }
        return result;
    }

    // 递归情况：固定一个数，递归解决 (k-1)Sum
    for (int i = start; i < n - k + 1; i++) {
        if (i > start && nums[i] == nums[i - 1]) continue;  // 去重

        List<List<Integer>> subResult = kSum(nums, target - nums[i], k - 1, i + 1);
        for (List<Integer> sub : subResult) {
            List<Integer> item = new ArrayList<>();
            item.add(nums[i]);
            item.addAll(sub);
            result.add(item);
        }
    }
    return result;
}

// 主函数（以 4Sum 为例）
public List<List<Integer>> fourSum(int[] nums, int target) {
    Arrays.sort(nums);
    return kSum(nums, target, 4, 0);
}
```

这个框架可以直接解决 3Sum（`k=3`）、4Sum（`k=4`）及更高阶的 kSum 问题，无需修改代码结构。

### 5.2 框架的复杂度分析

- 2Sum（有序+对撞指针）：$O(n)$
- 3Sum：$O(n) \times O(n) = O(n^2)$（1层递归）
- 4Sum：$O(n^2) \times O(n) = O(n^3)$（2层递归）
- kSum：$O(n^{k-1})$

递归深度 $k-2$（因为基础情况是 2Sum），栈深度 $O(k)$，但 $k$ 通常是常数，不计入复杂度分析。

---

## 第 6 章 面试常见追问

### 6.1 "3Sum 的时间复杂度能否优化到 $O(n \log n)$？"

不能（在比较模型下）。3Sum 本质上是在问：数组中是否存在三个元素之和为 0？这个问题有一个下界证明：在比较模型中，3Sum 的时间复杂度下界是 $\Omega(n^2)$（3SUM Hardness 理论），目前最好的算法也只能做到 $O(n^2 / \log n)$，$O(n^{1.5})$ 甚至更好的算法尚未被发现。

面试中如果被问到这个问题，可以说：$O(n^2)$ 已经是已知最优解的量级，$O(n \log n)$ 目前没有方案。

### 6.2 "如何不排序解决 3Sum？"

不排序的话，无法使用对撞指针，因为没有有序性保证。可以使用哈希表：对于每对 $(i, j)$，在哈希表中查找 `target - nums[i] - nums[j]`。但去重变得非常复杂（需要对结果三元组排序后用哈希集合去重），时间复杂度仍然是 $O(n^2)$，空间复杂度 $O(n)$。

综合来看，先排序再对撞指针的方案（$O(n^2)$ 时间，$O(\log n)$ 或 $O(n)$ 空间（取决于排序算法）），是工程上最优的方案。

### 6.3 "3Sum 中去重能不能用 HashSet 替代 while 去重？"

可以，但效率更低。用 `HashSet<List<Integer>>` 收集结果，自动去重。这种方案代码更简洁，但每次查询/插入 HashSet 需要 $O(1)$ 平均时间（但最坏 $O(n)$），且创建 `List<Integer>` 对象的内存分配开销更大。

排序后用 `while` 跳过重复值的方案，是 $O(1)$ 额外空间、$O(1)$ 时间完成去重判断，是更优的生产级代码。

---

## 小结

本文从 3Sum 的去重逻辑出发，深入分析了三处去重的每一处必要性，然后推广到 3Sum Closest 和 4Sum，最后抽象出通用的 kSum 递归框架。

核心收益：
- **降维思路**：$k$ 数之和 = 固定外层 + $(k-1)$ 数之和，最终退化为有序数组的 2Sum（对撞指针）
- **去重三原则**：外层与上一个元素比较（`nums[i-1]`）、内层跳过当前找到答案的重复值、两层循环各自独立去重
- **整数溢出**：多数之和时，记得转 `long`

下一篇将跳出"和/差"的约束，讨论对撞指针在几何意义更强的题目中的应用：接雨水（Trapping Rain Water）和盛最多水的容器（Container With Most Water）。这两道题都有基于几何直觉的贪心论证，是对撞指针正确性分析最精彩的案例之一。
