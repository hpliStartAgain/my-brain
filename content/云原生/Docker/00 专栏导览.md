---
title: "容器核心原理专栏导览"
date: 2026-03-04
tags: [cgroups, containerd, Docker, Linux, Namespace, OCI, UnionFS, 云原生, 容器]
aliases: []
---

# 容器核心原理专栏导览

## 专栏定位

本专栏**不是 Docker 使用教程**。`docker build`、`docker run`、Dockerfile 编写——这些内容互联网上已经汗牛充栋。本专栏关注的是 Docker 与所有容器技术**底层依赖的 Linux 内核机制**：当你执行 `docker run` 时，操作系统层面到底发生了什么？

容器的本质是**被 Linux 内核机制隔离和限制的进程**。Docker 的历史贡献在于把 Linux 内核中分散的隔离原语（Namespace、Cgroups、联合文件系统）封装为标准化的产品体验，但它没有发明任何新的隔离技术。理解这些内核机制，不仅能让你真正理解容器"为什么轻量"、"隔离边界在哪里"、"安全风险是什么"，更是后续学习 Kubernetes 的必备前置知识——K8s 的 Pod、资源限制、网络模型都直接建立在这些内核原语之上。

专栏的叙事主线是一条自底向上的拼图路径：**先认识容器本身（01），再依次展开它的三大支柱与网络（02-05），最后直面共享内核的安全代价（06）**。每篇都遵循同一套方法论：先讲"为什么会出现"（它解决什么、取代了什么），再讲"如何工作"（机制与可复现的手工实验），最后划定"边界与反例"（它不做什么、什么场景失效）。

## 目录

| 序号 | 文章 | 核心内容 |
| :--- | :--- | :--- |
| 01 | [[01 容器的本质——从进程隔离到 OCI 标准]] | 容器 vs 虚拟机的隔离层次差异、容器演进史（chroot → Jail → LXC → Docker → OCI）、OCI 三大规范、运行时分层架构（containerd / shim / runc）、`docker run` 的完整执行路径、K8s 移除 dockershim 的来龙去脉 |
| 02 | [[02 Linux Namespace 深度解析]] | 视图隔离思想与 nsproxy 内核实现、六大核心 Namespace 逐一拆解（PID/Mount/Network/UTS/IPC/User）、Cgroup 与 Time 新生代、clone/unshare/setns 三系统调用、手工搭建五重隔离环境、Pod 的 pause 容器机制 |
| 03 | [[03 Cgroups 资源限制与控制]] | v1 多层级 vs v2 统一层级、CPU 控制器（CFS 配额与 Throttling 陷阱）、Memory 控制器（Page Cache 计入、OOM Killer 与 QoS）、I/O 与 pids 控制器、Docker/K8s 参数映射、PSI 压力指标与 kubelet 驱逐 |
| 04 | [[04 UnionFS 与容器镜像原理]] | 独立 rootfs 的必要性、OverlayFS 深度解析（CoW/Whiteout/多层叠加）与手工实验、镜像的分层与内容寻址（digest/diffID 双哈希）、构建缓存与多阶段构建、Registry 分发与懒加载、Volume 划界与节点镜像管理 |
| 05 | [[05 容器网络原理]] | 容器网络的五类通信需求、veth pair 与 Linux Bridge（MAC 学习/FDB）、iptables NAT（MASQUERADE/DNAT/conntrack）、Docker 四种网络模式、三类流量的逐跳推演、跨主机与 VXLAN、CNI 规范与 K8s 扁平网络 |
| 06 | [[06 容器安全边界与逃逸风险]] | 共享内核的根本矛盾与威胁模型分档、多层防御模型、Capabilities/Seccomp/AppArmor/SELinux 四层机制、真实逃逸案例解剖（CVE-2019-5736 等）、Rootless 与安全容器（gVisor/Kata）、加固清单与 Pod Security Standards |

## 推荐阅读路径

- **完整主线**：01 → 02 → 03 → 04 → 05 → 06（每篇建立在前一篇的概念之上）
- **快速理解容器本质**：01 → 02（看完即可回答"容器和虚拟机的区别"的机制级原因）
- **容器性能问题排查**：03 → 05（CPU 限流与 OOM 是最常见的问题源，网络排障次之）
- **镜像与 CI/CD 优化**：04（构建缓存、多阶段构建、镜像瘦身的完整依据）
- **安全加固与多租户**：02 → 06（User Namespace 是 Rootless 的地基，先懂它再谈安全容器）
- **Kubernetes 前置**：01 → 02 → 05（Pod 沙箱、资源模型、CNI 的机制来源）

## 与后续专栏的关系

本专栏是 **Kubernetes 专栏群**的硬性前置。K8s 中的以下核心概念直接依赖本专栏的知识：

| K8s 概念 | 依赖的容器内核知识 | 建议前置 |
| :--- | :--- | :--- |
| Pod 的隔离模型（pause 容器、Namespace 共享） | Namespace 机制与 OCI 的 path 声明 | 01、02 |
| `resources.requests` / `resources.limits` | Cgroups 的权重与配额、oom_score_adj | 03 |
| 容器镜像拉取与存储 | OCI 镜像格式、内容寻址、Snapshotter | 01、04 |
| Pod 网络与 Service | Network Namespace、veth、NAT、CNI | 02、05 |
| Pod Security Standards | Capabilities、Seccomp、MAC | 06 |
| RuntimeClass 与安全容器 | OCI 运行时规范、gVisor/Kata | 01、06 |

## 关联专栏

- [[云原生/Kubernetes/kubernetes架构原则和对象设计/00 专栏导览|K8s 架构原则和对象设计]]：Pod 的隔离模型基于本专栏的 Namespace 知识
- [[云原生/Kubernetes/kubernetes网络原理与插件/00 专栏导览|K8s 网络原理与插件]]：veth、Bridge、CNI 依赖本专栏的容器网络基础
- [[云原生/服务网格/00 专栏导览|服务网格]]：Sidecar 模式依赖容器注入机制，Envoy 流量治理建立在容器网络之上
- [[Linux/进程管理/00 专栏导览|进程管理专栏]]：Namespace、clone/fork 的内核基础
- [[Linux/文件系统/00 专栏导览|文件系统专栏]]：OverlayFS、Page Cache 的底层原理
- [[Linux/网络协议栈与IO/09 容器网络原理——veth、bridge、iptables 与 eBPF|容器网络原理（协议栈视角）]]：同一主题在 Linux 网络栈维度的深入展开
